{% extends "BthuillierMainBundle:Blog:show.html.twig" %}
{% block page_name %}
    {{ parent() }}
    {% if is_granted('ROLE_ADMIN') %}
        <a href="{{path('bthuillier_main_blog_list')}}" class="btn primary pull-right">Retour</a>
    {% endif %}

{%endblock%}